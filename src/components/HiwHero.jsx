import React from 'react';
import './HiwHero.css';

const HiwHero = () => {
  return (
    <section id="hiw-hero" className="hiw-hero-section">
      <div className="container text-center">
        <h1 
          className="hiw-hero-title gradient-text animate-on-scroll fade-up"
          
          
          
        >
          From first visit to loyal superfan — automatically.
        </h1>
        <p 
          className="hiw-hero-sub"
          
          
          
        >
          No manual processes. No missed moments. Just a loyalty engine that runs itself.
        </p>
      </div>
      <div className="hiw-hero-glow" />
    </section>
  );
};

export default HiwHero;
